import { height } from 'react-native-dimension'

const appStyles = {
  textReqular: {
    fontSize: height(2.3),
  },
  textMaxi: {
    fontSize: height(4),
    fontWeight: 'bold',
  },
}

export default appStyles
